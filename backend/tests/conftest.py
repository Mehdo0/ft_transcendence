"""Test configuration for ft_transcendence backend."""

import sys
from pathlib import Path

# Add the backend directory to the Python path so tests can import backend modules
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
