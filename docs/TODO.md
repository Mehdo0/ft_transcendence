
|-----------------------RULES------------------------------------------|
|- If you're working on a task, note you're name in front of it !      |
|- If you need something to be done, add a task.                       |
|- If a task has to be done quickly, add <!important> in front of it ! |
|- Prioritize the <!important> tasks.                                  |
|-----------------------RULES------------------------------------------|

- [ ] Create a container to store the frontend (dependencies, installation etc...) 

- [X] Create the database with the correct colons (user_name, user_id, elo, wins, losses, friends)

- [X] Code a simple function in FastApi to ask specific data to the database using an ORM like SQLAlchemy

- [X] Code a FastApi function to format the DB data into JSON response e.g: (14,5) -> {"wins": 14, "losses": 5}

- [X] Code a function to add data into the DB

- [ ] Connect front to backend (started by lfaure)
    - [ ] simple API fetch
    - [ ] WebSocket connection
    - [ ] vector data transfer (one block)
    - [ ] vector data stream

- [ ] Train the AI following the instructions @AI_DOCS.md

- [ ] Code the connection between the backend and the AI

- [ ] Code the visuals in SvelteKit of different pages 

- [ ] Code every URL address in FastApi to fetch users data / add data to the DB, like "/api/user/{"user_id"}/elo" to get an user's Elo

- [ ] List every data that is needed by Svelte 

- [ ] Implement an authentication service

- [ ] Implement a matchmaking service 

- [ ] ...
